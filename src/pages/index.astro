---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const quizzes = await getCollection('quizzes');
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<Layout title="クイズサイト">
  <h1>🎯 クイズサイト</h1>
  <p>Astroで構築された静的クイズサイトへようこそ！</p>
  
  <div class="quiz-list">
    <h2>利用可能なクイズ</h2>
    {quizzes.map((quiz) => (
      <div class="quiz-card">
        <h3>{quiz.data.title}</h3>
        <p>{quiz.data.description}</p>
        <p class="question-count">問題数: {quiz.data.questions.length}</p>
        <a href={`${base}/quiz/${quiz.id}`} class="start-button">開始する</a>
      </div>
    ))}
  </div>
</Layout>

<style>
  /* CSS Variables for consistent theming */
  :root {
    --primary-color: #667eea;
    --primary-dark: #764ba2;
    --text-primary: #333;
    --text-secondary: #666;
    --bg-light: #f8f9fa;
    --border-color: #e0e0e0;
    --spacing-xs: 10px;
    --spacing-sm: 15px;
    --spacing-md: 20px;
    --spacing-lg: 30px;
    --border-radius-sm: 6px;
    --border-radius-md: 8px;
    --transition-normal: 0.3s;
    --shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.1);
    --shadow-md: 0 4px 12px rgba(102, 126, 234, 0.2);
    --shadow-lg: 0 4px 12px rgba(102, 126, 234, 0.4);
  }

  .quiz-list {
    margin-top: var(--spacing-lg);
  }

  .quiz-list h2 {
    margin-bottom: var(--spacing-md);
    font-size: 1.8rem;
    color: var(--primary-color);
  }

  .quiz-card {
    background: var(--bg-light);
    border-radius: var(--border-radius-md);
    padding: var(--spacing-md);
    margin-bottom: var(--spacing-md);
    border-left: 4px solid var(--primary-color);
    transition: transform var(--transition-normal), box-shadow var(--transition-normal);
    box-shadow: var(--shadow-sm);
  }

  .quiz-card:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }

  .quiz-card h3 {
    color: var(--text-primary);
    margin-bottom: var(--spacing-xs);
    font-size: 1.3rem;
  }

  .quiz-card p {
    color: var(--text-secondary);
    margin-bottom: var(--spacing-xs);
    line-height: 1.6;
  }

  .question-count {
    font-weight: 600;
    color: var(--primary-color);
    font-size: 0.9rem;
  }

  .start-button {
    display: inline-block;
    background: linear-gradient(135deg, var(--primary-color) 0%, var(--primary-dark) 100%);
    color: white;
    padding: 12px 28px;
    min-height: 48px;
    max-width: 100%;
    box-sizing: border-box;
    border-radius: var(--border-radius-sm);
    margin-top: var(--spacing-xs);
    font-weight: 600;
    transition: transform var(--transition-normal), box-shadow var(--transition-normal);
    box-shadow: var(--shadow-sm);
    text-align: center;
  }

  .start-button:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
    color: white;
  }

  .start-button:active {
    transform: translateY(0);
  }

  /* Mobile optimizations */
  @media (max-width: 600px) {
    .quiz-card {
      padding: 18px;
    }

    .start-button {
      display: block;
      width: 100%;
      padding: 14px 24px;
      min-height: 52px;
    }
  }
</style>
